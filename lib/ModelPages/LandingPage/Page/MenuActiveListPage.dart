import 'package:flutter/material.dart';

class MenuActiveListPage extends StatelessWidget {
  const MenuActiveListPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
