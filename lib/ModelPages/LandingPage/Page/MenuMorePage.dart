import 'package:flutter/material.dart';

class MenuMorePage extends StatelessWidget {
  const MenuMorePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
